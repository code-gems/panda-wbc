// utils
import { css } from "lit";

export const styles = css`
	:host {
		display: block;
		width: 100%;
		height: 100%;
	}

	.actions {
		display: flex;
		gap: var(--panda-gap-4);
		margin-top: var(--panda-gap-8);
	}
`;