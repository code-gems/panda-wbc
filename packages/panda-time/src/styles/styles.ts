import { css } from 'lit';

export const styles = css`
  .clock-container {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 20px;
  }

  .clock {
    position: relative;
  }

  .clock text {
    font-size: 8px;
    fill: black;
  }
`;

export const overlayStyles = /*css*/`

`;