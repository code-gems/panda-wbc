export const styles = /*css*/`
	:host {
		display: inline;
		width: fit-content;
	}

	.time-input {
		display: block;
		min-width: .5rem;
		outline: none;

		color: inherit;
		font-size: inherit;
		font-family: inherit;
		text-shadow: inherit;
		line-height: inherit;
		caret-color: transparent;
		cursor: text;
		white-space: nowrap;

		box-sizing: border-box;
	}
`;