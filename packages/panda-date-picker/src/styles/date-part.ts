export const styles = /*css*/`
	:host {
		color: inherit;
		font-size: inherit;
		font-family: inherit;
		min-width: 2rem;
	}

	span {
		color: inherit;
		font-size: inherit;
		font-family: inherit;
		outline: none;
	}
`;