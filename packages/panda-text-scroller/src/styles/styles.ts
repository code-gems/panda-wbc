export const styles = /*css*/`
	:host {
		position: relative;
		display: inline-block;
		overflow: hidden;
	}

	.track {
		display: inline-block;
		
		color: inherit;
		font-family: inherit;
		font-size: inherit;
		text-shadow: inherit;
		white-space: nowrap;
		
		will-change: transform;
		box-sizing: border-box;
	}
`;