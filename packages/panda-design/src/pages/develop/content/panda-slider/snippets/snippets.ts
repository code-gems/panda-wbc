export const installationSnippet = `npm install @panda-wbc/panda-slider -S`;

export const implementationSnippet = `
import "@panda-wbc/panda-slider";

<panda-slider
  .min="\${0}"
  .max="\${100}"
  .value="\${50}"
>
</panda-slider>
`;
