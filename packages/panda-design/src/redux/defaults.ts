// types
import { AppState } from "panda-design-typings";

export const DEFAULT_STORE: AppState = {
	currentPageDetails: {
        pathname: "/",
        search: "",
        searchParams: {}	
    }
};